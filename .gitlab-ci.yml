image: alpine

.build: &build
  script:
    - mkdir public
    - cp index.html public/
    - cp -r js public/
    - cp -r css public/
    - cp -r static public/
  artifacts:
    paths:
      - public

pages:
  <<: *build
  only:
    - main

preview:
  <<: *build
  except:
    - main
